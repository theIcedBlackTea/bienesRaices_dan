extends ../layout/index

block contenido
    div.py-10
        h1.text-4xl.my-10.font-extrabold.text-center Subir Imagen de Perfil
        form(method="POST", action="auth/guardarImagen/" + id, enctype="multipart/form-data")
            input(type="file", name="file")
            input(type="submit", value="Subir Imagen")
